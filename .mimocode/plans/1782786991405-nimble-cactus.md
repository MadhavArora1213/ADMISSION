# Plan: Make notifications_dashboard.php Responsive

## Problem
The page has two duplicate style blocks (minified CSS) and the 2-column grid layout (`grid-template-columns: 1fr 1fr`) doesn't stack on mobile.

## File to Modify
- `C:\xampp\htdocs\ADMISSION\admin\notifications_dashboard.php`

## Changes

### 1. Replace both style blocks with single clean CSS
Remove the two `<style>` blocks (lines 21-29) and replace with one clean block containing:
- All base styles (sidebar, topbar, content, panels, tables, etc.)
- Mobile menu button and sidebar overlay styles
- `.grid-2` class for the 2-column layout
- Media queries for 768px and 480px breakpoints

### 2. Update HTML grid class
Change line 66 from:
```html
<div style="display:grid; grid-template-columns: 1fr 1fr; gap:24px;">
```
To:
```html
<div class="grid-2">
```

### 3. Update table panel
Change the table wrapper from inline `overflow-x:auto` to use `.panel-body` class.

## Verification
- Open in browser at 375px width
- Sidebar should hide and show via hamburger menu
- Stats grid should go 4 cols → 2 cols → 1 col
- 2-column panels should stack to single column
- Table should scroll horizontally
- Sub-links should wrap
